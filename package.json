{
  "name": "techflow",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "start cmd /k \"npm run dev --prefix backend\" && start cmd /k \"npm run dev --prefix frontend\"",
    "db:up": "docker-compose up -d",
    "db:down": "docker-compose down"
  }
}
